kind: BPServiceCI
metadata:
  application: aqualens
  environment_master: staging
  service: aqualens-b2b
spec:
  application_env: pre-prod
  branch_name: master
  dockerfile_path: Dockerfile:.
  env_ci_build_variable:
  - env_key: SVC
    env_value: aqualens-b2b
  git_repo: ot-micro-salary
  language: PHP
version: api/v1
