kind: BPApplicationEnv
metadata:
  name: pre-prod
spec:
  application: payments
  cluster: ot-aws-cluster
  environment_master: staging
  manual_build: true
  manual_deploy: true
  namespace: dev
  registries:
  - bp
  vm_groups: []
version: api/v1
